# Security Model

TLS in transit; encryption at rest; least privilege; separate device/user identity; no secrets in source or images; hardware control OFF by default; raw media OFF by default; audit mutating requests; schema validation; dependency/SAST/container scanning; SBOM per release; no financial execution path.
