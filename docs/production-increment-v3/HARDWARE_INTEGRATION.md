# Hardware Integration

Flow: procurement verification -> incoming inspection -> electrical bring-up -> sensor transport -> TEC thermal characterization -> optical alignment -> dark/flat-field characterization -> photon-statistics characterization -> FPGA/reference equivalence -> end-to-end reconstruction -> safety review -> acceptance.

A software field is never labelled a measured photon count until the sensor calibration chain and acquisition provenance are verified.
