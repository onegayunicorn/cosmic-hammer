# J09 S runtime smoke path

Run `pnpm smoke:sovereign-lattice-runtime` to exercise the connected software path. It uses an in-memory simulated adapter, J09-style twin state, deterministic lattice derivation, bridge/bell coordination, and a local hash ledger. It does not connect to BLE/NFC, flash firmware, collect phone identifiers, or write to external systems.
