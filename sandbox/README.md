# Sandbox

The sandbox is isolated, resettable, exportable, and configured with external writes disabled. Prediction outputs are deterministic demo traces and should not be used for operational decisions.
